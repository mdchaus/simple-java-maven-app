pipeline {
    agent any
    stages {
        stage('Run Command') {
            steps {
                sh 'echo "Hello from Jenkins Server!"'
                sh 'ls -l'
            }
        }
    }
}
